services:
  - type: web
    name: cleancity-ai
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn clean_city_ai.app:app --host 0.0.0.0 --port $PORT --proxy-headers --forwarded-allow-ips='*'
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
